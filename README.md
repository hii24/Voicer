# Voicer

Next.js app for the Voicer UI with Firebase auth and admin controls.

## Setup

1. Install dependencies:
   `npm install`
2. Create `.env.local` with Firebase config and API keys.
3. Run the dev server:
   `npm run dev`
